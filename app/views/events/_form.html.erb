<%= simple_form_for @event, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nom_evento, label: 'Nombre Evento' %>
  <%= error_span(@event[:nom_evento]) %>

  <%= f.input :fecha, label: 'Fecha / Hora Evento' %>
  <%= error_span(@event[:fecha]) %>
  <%= f.input :capacidad, label: 'Cantidad Personas' %>
  <%= error_span(@event[:capacidad]) %>
  <%= f.input :descripcion, label: 'Descripción' %>
  <%= error_span(@event[:descripcion]) %>


  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                events_path, :class => 'btn btn-default' %>
<% end %>
